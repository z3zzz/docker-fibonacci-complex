sudo: required
services:
  - docker
  - postgresql
  - redis-server

before_install: 
  - psql -c 'CREATE TABLE values (number INTEGER);' -U postgres
  - psql -c "ALTER USER postgres PASSWORD '1234';" -U postgres
  - docker build -f ./api/Dockerfile -t kwangduck/docker-fibonacci-complex-api-test ./api
      

script:
  - docker run kwangduck/docker-fibonacci-complex-api-test

language: generic
